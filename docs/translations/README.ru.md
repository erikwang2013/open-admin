> Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz
>
> [中文](../README.md) | [English](README.en.md) | [한국어](README.ko.md) | [Русский](README.ru.md) | [Deutsch](README.de.md) | [Français](README.fr.md) | [Español](README.es.md) | [Português](README.pt.md) | [हिन्दी](README.hi.md) | [العربية](README.ar.md) | [বাংলা](README.bn.md) | [Bahasa Indonesia](README.id.md) | [日本語](README.ja.md)

# Открытая админ-панель (open-admin)

Полнофункциональная система администрирования на базе webman v2 + Flutter.

> [English](README.en.md) | [한국어](README.ko.md) | [Русский](README.ru.md) | [Deutsch](README.de.md) | [Français](README.fr.md) | [Español](README.es.md) | [Português](README.pt.md) | [हिन्दी](README.hi.md) | [العربية](README.ar.md) | [বাংলা](README.bn.md) | [Bahasa Indonesia](README.id.md) | [日本語](README.ja.md) | [Схема архитектуры](docs/ARCHITECTURE.ru.md) | [Проектная документация](docs/DESIGN.ru.md) | [Архитектура безопасности](docs/SECURITY.ru.md) | [Справочник API](docs/API.ru.md)

## Перечень возможностей

| Бизнес-домен | Функция | Описание |
|--------|------|------|
| 🔐 Аутентификация | Вход / обновление токена / выход | Капча по клику + JWT + чёрный список |
| | Блокировка учётной записи | 5 неудачных попыток — блокировка на 15 минут |
| | Ограничение одновременных сессий | Не более 3 действующих Token на одного пользователя |
| 📊 Дашборд | Реальная статистика / графики трендов / диаграммы распределения / последние операции | Кэш Redis на 5 минут |
| 👥 Пользователи | CRUD + массовое удаление / включение и отключение | Мягкое удаление + повторное подтверждение паролем |
| | Массовый импорт Excel | Построчная валидация + отчёт об ошибках |
| 🔒 Роли и права | CRUD ролей + дерево прав | Авторизация RBAC с точностью method.path |
| ⚙ Системные настройки | CRUD пар «ключ-значение» | Группировка |
| 📋 Аудит операций | Запрос журнала + определение источника | Автоматическое распознавание 8 платформ |
| 📁 Файлы | Загрузка / экспорт Excel / экспорт PDF | Автоматическая маскировка чувствительных данных |
| 🛡 Защита | 18 уровней эшелонированной обороны | XSS/SQL-инъекции/обход путей/инъекции команд/CSRF/лимит запросов/CSP... |
| 🏥 Эксплуатация | Health check / метрики / документация API / security.txt | Prometheus + OpenAPI 3.0 + интерактивная документация hg/apidoc |
| 🌐 Локализация | Переключение китайского и английского | Заголовок Accept-Language / параметр ?lang= |

## Технологический стек

| Слой | Технология | Описание |
|---|------|------|
| Бэкенд-фреймворк | webman v2 (workerman) | Сверхпроизводительный PHP-фреймворк с резидентными процессами |
| Версия PHP | 8.3+ | |
| База данных | MySQL 8.0+ | Префикс таблиц `erik_`, BIGINT-первичные ключи без автоинкремента |
| Поисковый движок | Elasticsearch | Синхронизация и поиск через `webman-scout` |
| Фронтенд админки | Flutter 3.x | Веб-версия в стиле PC-панели управления (`apps/flutter/`) |
| Мобильные клиенты | HarmonyOS ArkTS | Нативный клиент HarmonyOS (`apps/harmonyos/`): телефоны/планшеты/2in1 |

## Ключевые зависимости

| Пакет | Назначение |
|---|------|
| `erikwang2013/snowflake-php` | Генерация глобально уникальных BIGINT-первичных ключей по алгоритму Snowflake |
| `erikwang2013/hashids` | Шифрование ID на уровне API, скрытие реальных ID базы данных |
| `erikwang2013/jwt-webman` | Выпуск и проверка JWT-токенов аутентификации |
| `erikwang2013/encryption` | Шифрование чувствительных данных на транспортном уровне |
| `erikwang2013/encryptable` | Автоматическое шифрование чувствительных полей на уровне БД |
| `erikwang2013/webman-scout` | Синхронизация данных с Elasticsearch и полнотекстовый поиск |
| `erikwang2013/season` | Данные флагов стран |
| `erikwang2013/poster-php` | Генерация и проверка капчи по клику + генерация постеров |
| `phpoffice/phpspreadsheet` | Экспорт Excel |
| `barryvdh/laravel-dompdf` | Экспорт PDF (на основе Dompdf) |

## Структура проекта

```
open-admin/
├── app/
│   ├── admin/controller/       # Контроллеры админки
│   │   ├── DashboardController.php # Дашборд (кэш Redis)
│   │   ├── UserController.php      # CRUD пользователей + массовые операции
│   │   ├── RoleController.php      # CRUD ролей
│   │   ├── PermissionController.php# CRUD прав
│   │   ├── ConfigController.php    # CRUD системных настроек
│   │   ├── LogController.php       # Запрос журнала операций
│   │   ├── ProfileController.php   # Личный кабинет + выход
│   │   ├── ExportController.php    # Экспорт Excel/PDF
│   │   ├── ImportController.php    # Импорт пользователей из Excel
│   │   ├── UploadController.php    # Загрузка файлов
│   │   ├── HealthController.php    # Проверка работоспособности
│   │   ├── DocsController.php      # OpenAPI-документация
│   │   └── BaseController.php      # Базовый контроллер
│   ├── api/
│   │   └── v1/controller/          # Контроллеры API v1 (версия задаётся заголовком API-Version)
│   │       ├── CaptchaController.php # Капча по клику
│   │       └── AuthController.php    # Вход / обновление токена
│   ├── common/                 # Общие утилиты
│   │   ├── HashidsService.php  # Кодирование/декодирование ID
│   │   ├── SnowflakeService.php# Генерация ID Snowflake
│   │   └── EncryptionService.php # Шифрование данных + маскирование
│   ├── middleware/             # Промежуточное ПО
│   │   ├── Cors.php            # CORS
│   │   ├── SecurityFilter.php  # Перехват атак (ограничение методов HTTP/XSS/SQL-инъекции/обход путей/инъекции команд/CSRF)
│   │   ├── RateLimit.php       # Ограничение частоты запросов Redis (скользящее окно + заголовки ответа)
│   │   ├── ApiVersion.php      # Проверка версии API
│   │   ├── AdminAuth.php       # Аутентификация JWT + чёрный список
│   │   ├── AdminPermission.php # Проверка прав RBAC
│   │   └── OperationLog.php    # Автоматическая запись журнала операций (с определением источника)
│   └── model/                  # Модели данных
├── apps/
│   ├── flutter/                # Flutter Web-админка (PC-стиль)
│   │   └── lib/app/
│   │       ├── pages/          # 5 полных страниц (дашборд/пользователи/роли/настройки/журнал/кабинет)
│   │       ├── services/       # ApiService (JWT-перехватчик) + AuthService (персистентность токена)
│   │       └── layouts/        # Адаптивный макет админки (сайдбар + верхняя панель + контент)
│   └── harmonyos/              # Нативный клиент HarmonyOS (бесшовное обновление токена)
├── config/                     # Конфигурация (с китайскими комментариями)
│   ├── route.php               # Маршруты + политика версий API
│   ├── middleware.php           # Регистрация глобального промежуточного ПО
│   └── ...                     # Конфигурации компонентов
├── database/install.sql        # SQL-скрипт установки (с данными прав)
├── public/                     # Точка входа
├── runtime/                    # Файлы времени выполнения
└── vendor/                     # Зависимости Composer
```

## Требования к окружению

- PHP >= 8.3
- Composer 2.x
- MySQL >= 8.0
- Flutter >= 3.41 (только для разработки фронтенда)
- Elasticsearch >= 7.x (необязательно, требуется для поиска)

## Быстрый старт

### 1. Установка зависимостей

```bash
composer install
```

### 2. Настройка переменных окружения

Скопируйте и отредактируйте переменные окружения (необязательно: если они не заданы, используются значения по умолчанию из `config/*.php`):

```bash
cp .env.example .env
```

Ключевые параметры:

| Переменная окружения | Описание | Значение по умолчанию |
|---------|------|--------|
| `JWT_SECRET` | Секрет подписи JWT | `open-admin-jwt-secret-change-in-production` |
| `HASHIDS_SALT` | Соль Hashids | `open-admin-hashids-salt-2026` |
| `ENCRYPTION_KEY` | Ключ шифрования API | 32-байтовое значение по умолчанию |
| `SNOWFLAKE_DATACENTER_ID` | ID дата-центра (0-31) | `1` |
| `SNOWFLAKE_WORKER_ID` | ID рабочего узла (0-31) | `1` |
| `SCOUT_HOSTS` | Адрес ES | `http://localhost:9200` |

**В производственной среде обязательно замените все ключи на случайные строки.**

### 3. Установка в один клик

После запуска сервиса откройте в браузере мастер установки, чтобы инициализировать базу данных и создать администратора:

```bash
php start.php start
```

По умолчанию сервис слушает `http://0.0.0.0:8787` (порт можно изменить в `config/server.php`).

Откройте в браузере **`http://localhost:8787/install`** и заполните поля мастера:

| Шаг | Содержимое |
|------|------|
| ① Конфигурация БД | Хост, порт, имя БД, пользователь, пароль |
| ② Настройки администратора | Имя пользователя и пароль администратора (по умолчанию admin / admin888) |

После нажатия «Начать установку» автоматически создаются таблицы, заполняются данные прав, создаётся учётная запись администратора и в `.env` записывается конфигурация БД.

> После установки создаётся файл блокировки `runtime/install.lock`. Для повторной установки удалите этот файл.

### 4. Вход в систему

Откройте `http://localhost:8787` и войдите с учётными данными администратора, заданными при установке.

### 5. Запуск фронтенда (необязательно)

**Flutter-админка (Web):**

```bash
cd apps/flutter
flutter pub get
flutter run -d chrome    # Веб-версия (в стиле PC-панели управления)
```

**Клиент HarmonyOS (мобильный):**

Откройте каталог `apps/harmonyos/` в DevEco Studio и запустите на реальном устройстве или эмуляторе.

### 6. Развертывание в один клик через Docker Compose (рекомендуется для продакшена)

Проект содержит полную Docker-оркестрацию из 5 сервисов: Nginx, PHP (приложение webman), MySQL, Redis, Elasticsearch.

```bash
# 1. Настройте переменные окружения для Docker
cp .env.docker .env

# 2. Запустите все сервисы
docker-compose up -d

# 3. Откройте мастер установки в браузере
# http://localhost:8787/install  (укажите данные БД и администратора)
# или выполните SQL-миграцию вручную (внутри контейнера app):
# docker-compose exec app mysql -h mysql -u root -p < database/install.sql

# 4. Доступ
# http://localhost:8787  (webman)
# http://localhost:8080  (Nginx reverse proxy)
```

- `Dockerfile`: PHP 8.3 + OPcache + Composer, на базе `php:8.3-cli`
- `docker-compose.yml`: оркестрация 5 сервисов, изоляция сети, персистентные тома данных
- `.env.docker`: переменные окружения для Docker


## Стандарты базы данных

- **Префикс таблиц**: `erik_`
- **Первичный ключ**: во всех таблицах — `id BIGINT UNSIGNED NOT NULL`, **AUTO_INCREMENT запрещён**
- **Генерация ID**: первичные ключи генерируются на уровне приложения через `SnowflakeService::generate()`, уникальны в распределённой среде
- **Обязательные поля**: каждая таблица обязана содержать `id`, `created_at`, `updated_at`
- **Мягкое удаление**: для таблиц с мягким удалением добавляется `deleted_at DATETIME DEFAULT NULL`
- **Чувствительные поля**: телефон, email, номер удостоверения и т. п. автоматически шифруются/расшифровываются плагином `encryptable`; в БД хранится шифротекст в `VARCHAR(500)`

## Документация API

Полный справочник API (единый формат ответов, коды ошибок, все эндпоинты, процесс аутентификации, политика лимитов, цепочка промежуточного ПО) — см. **[docs/API.ru.md](docs/API.ru.md)**. Ключевые моменты:

- **Единый формат ответа**: `{ "code": 0, "message": "success", "data": {...} }`, `code=0` означает успех
- **Коды ошибок**: `400` ошибка параметров / `401` не авторизован / `403` нет прав / `404` не найдено / `422` ошибка валидации / `429` превышен лимит запросов / `500` ошибка сервера
- **Версия API**: задаётся заголовком `API-Version: v1` (по умолчанию v1), в URL не отражается
- **Аутентификация**: `Authorization: Bearer <token>`; access_token действует 2 часа, refresh_token — 14 дней
- **Работа с ID**: ID в запросах/ответах — строки, зашифрованные hashids; реальные ID БД не раскрываются

## О фронтенде

### Flutter-админка (PC-стиль)

- **Макет**: боковая панель (сворачивается 64px/240px) + верхняя панель + область контента, три адаптивные точки (телефон/планшет/десктоп)
- **Страницы**: вход, дашборд, пользователи, роли и права, системные настройки, журнал операций, личный кабинет
- **Управление состоянием**: GetX (синглтон `ApiService` + `AuthService` для персистентности токена)
- **Дашборд**: карточки статистики, линейные графики трендов (fl_chart), круговая диаграмма, последние операции
- **Экспорт**: Excel/PDF; PDF содержит неудаляемую информацию об авторских правах
- **Массовые операции**: массовое удаление по выбору, массовое включение/отключение
- **Тема**: Material 3, светлая/тёмная

### Мобильный клиент HarmonyOS

- **Страницы**: вход, дашборд, список/детали пользователей, личный кабинет
- **Аутентификация**: JWT Bearer + автоматическое бесшовное обновление токена при 401; при неудаче — редирект на страницу входа
- **Хранение**: токен через AppStorage

## Правила разработки

- Глобальные функции/классы — без ведущего `\`, импорт только через `use`
- Каждый PHP-файл должен содержать заголовок с заявлением об авторских правах
- Все конфигурационные файлы должны содержать китайские комментарии с пояснением параметров
- Первичные ключи БД генерируются snowflake на уровне приложения; автоинкремент запрещён
- Все ID в параметрах и ответах API должны шифроваться/расшифровываться через hashids
- Middleware AdminPermission кэширует права пользователя в Redis (TTL=60s), устраняя узкое место запросов N+1

## Развертывание

### Docker Compose (рекомендуется)

В корне проекта — `docker-compose.yml` с оркестрацией 5 сервисов:

| Сервис | Образ | Порт |
|------|------|------|
| `nginx` | nginx:alpine | 80, 443 |
| `app` | локальная сборка из `Dockerfile` | 8787 |
| `mysql` | mysql:8.0 | 3306 |
| `redis` | redis:7-alpine | 6379 |
| `elasticsearch` | elasticsearch:8.x | 9200 |

PHP-образ собирается из `Dockerfile`, базовый образ `php:8.3-cli`, включён OPcache.

```bash
cp .env.docker .env
docker-compose up -d
```

### CI/CD

Пайплайн непрерывной интеграции GitHub Actions: `.github/workflows/ci.yml`

- Проверка синтаксиса PHP (`php -l`)
- Модульные тесты PHPUnit
- Статический анализ Flutter (`flutter analyze`)

### Резервное копирование БД

Каталог `database/backup/`:

- `backup.sh` — резервное копирование mysqldump + gzip, автоматическая очистка бэкапов старше 30 дней
- `restore.sh` — интерактивное восстановление со списком доступных бэкапов

### Безопасная конфигурация Nginx

Для продакшена см. `docs/nginx-security.conf` — рекомендации по усилению безопасности обратного прокси.

## Открытый исходный код — поддержите проект

| WeChat | Alipay |
|:---:|:---:|
| ![WeChat](./docs/weixinpay.png "WeChat") | ![Alipay](./docs/alipay.png "Alipay") |

### Донат международным переводом (кросс-граничный перевод)

**Информация о получателе**

- Имя получателя: WANG KEXUN
- Номер счёта получателя: 881015918251

**Банк получателя**

- SWIFT-код ZA Bank: AABLHKHHXXX
- Название банка: ZA Bank Limited
- Код банка: 387
- Адрес банка: Core F, Cyberport 3, 100 Cyberport Road, Hong Kong

**Банк-посредник для международных переводов (при необходимости)**

> Это информация о банке-посреднике (транзитном банке), а не о банке получателя. Уточните в банке отправителя, требуется ли предоставлять данные банка-посредника.

- **Для переводов в гонконгских долларах, юанях и долларах США** банк-посредник — Citibank:
  - Название банка: Citibank N.A. Hong Kong
  - SWIFT-код: CITIHKHXXXX
  - Код банка: 006
  - Название отделения: Hong Kong Branch
  - Код отделения: 391
  - Адрес банка: Citibank Tower, Citibank Plaza, 3 Garden Road, Central, Hong Kong
- **Для переводов в других валютах** банк-посредник — BNY Mellon:
  - Название банка: THE BANK OF NEW YORK MELLON
  - SWIFT-код: IRVTUS3NXXX
  - Адрес банка: THE BANK OF NEW YORK MELLON, 240 GREENWICH STREET, NEW YORK, United States

---

## Лицензия

MIT

Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz
